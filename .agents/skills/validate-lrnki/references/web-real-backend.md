# Real-backend web

Use [the owning rig contract](../../../../apps/learner-app/e2e-realuse/README.md) for preflight,
commands, environment, journey scopes, and exact cleanup. It exercises the production-format Expo
artifact with local Better Auth, Hono, server-side grading, and development Postgres.

When triaging the first API/browser error, distinguish harness startup, stale cookies,
content-revision refusal, and product behavior. Report only the journeys and UI scopes actually run.
