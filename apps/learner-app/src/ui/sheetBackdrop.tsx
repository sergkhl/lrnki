// Native Expo sheets own their system backdrop. The web implementation is selected by Metro.
export function BottomSheetBackdrop(props: Readonly<{ dismissBlocked: boolean; onDismiss: () => void }>) {
  void props;
  return null;
}
