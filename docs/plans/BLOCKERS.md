# Blockers

- **Choose the Source-less Grounding Admission U4 workaround.** Recommended: retire its sole bounded
  regeneration attempt, keep one initial draft, and preserve each consumer's current fail-closed
  treatment of a rejected outcome. This removes the prompt-only repair loop at the cost of lower
  neural recall. If that recall loss is unacceptable, request a separate design for independently
  verified predicate conservation; the current U4 plan will remain stopped and no live gate will be
  rerun in the meantime.
