# Blockers

- **Choose the Topic Expedition U2 route contract.** The exact provider matrices and no-seed
  diagnosis are recorded in the
  [active DeepSeek cutover plan](./2026-08-23-001-cut-over-topic-expedition-generation-to-deepseek-flash.md).
  Recommended: authorize a plan amendment that empirically qualifies a provider-pinned MiMo route
  without OpenRouter's `require_parameters` catalog filter, retains the client's documented
  best-effort seed, and initially uses the fully passed single-provider DeepSeek and GPT routes.
  Otherwise direct a new supporting-judge Model Assignment design or abandon the cutover. No more
  provider calls, config edits, reloads, or U3 work occur until this choice is recorded.
