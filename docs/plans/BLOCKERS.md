# Blockers

## Source-less Grounding identity-scope repair

- **Owner action:** decide whether to authorize a successor plan that may deepen the domain-neutral
  Grounding prompt or forced-tool contract with an enforced identity-scope and counterexample audit
  before the single initial draft. Preserve cross-family, draft-blind verification and its
  replicated-rejection veto; do not add retry-until-lucky regeneration.
- **Why owner-only:** [Plan 003](./2026-08-23-003-unify-source-less-grounding-on-deepseek.md) locks
  the current assignment and explicitly excludes prompt/schema, regeneration, retry, and admission
  policy changes. Its first required affected-consumer run found the producer defect and closed U4
  `FIX_FIRST`, so expanding that contract would exceed the approved implementation scope.
- **Drain criterion:** the owner explicitly names the permitted contract seam and preserves the
  one-draft invariant; then remove this manual blocker and add the successor to execution order. Its
  resulting candidate must still pass exact direct routes and Plan 003's complete U4 matrix before
  the latency plan resumes.
