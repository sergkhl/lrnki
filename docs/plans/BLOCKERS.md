# Blockers

- **Grounding Generation remediation scope:** In the planning interview for
  [Generation model evaluation](../brainstorms/2026-08-08-002-generation-model-evaluation.md), decide
  whether a new plan may change source-less Grounding Generation beyond the active latency plan's
  locked prompt boundary. The current judges rejected bounded prompt/schema and pre-draft trials;
  until scope is accepted and planned, latency-plan U3 remains `FIX_FIRST` and U4 stays gated.
